package com.example.pathpulse.constans

/**
 * Časový limit (ms) pre FlowSharing a debouncing.
 */
const val TIMEOUT_MILLIS = 5_000L //flow timeout millis

/**
 * Celkový počet krajín na svete (v Databáze)
 */
const val TOTAL_COUNTRIES = 195
